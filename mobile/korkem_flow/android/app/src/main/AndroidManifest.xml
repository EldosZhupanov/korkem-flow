<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app is a client for a Frappe/ERPNext server; without this a
         release build has no network at all. Flutter's template declares it
         only in the debug and profile manifests, so a debug build works and
         the release APK is dead on arrival. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Диктовка в ассистенте. Владелец описал продукт словами «цифровой
         администратор, которому я могу сказать всё голосом», и без этой строки
         сказать нельзя ничего: `speech_to_text` спрашивает у платформы, умеет
         ли она распознавать речь, платформа без разрешения отвечает «нет», а
         композер честно прячет кнопку, которая не может работать. Снаружи это
         выглядит как отсутствующая функция, хотя код на месте.
         Разрешение спрашивается при первом нажатии на микрофон, а не при
         запуске: просить доступ к микрофону у человека, который ещё ничего не
         сделал, — верный способ получить отказ навсегда. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Уведомления. С Android 13 это отдельное разрешение, и без него
         система молча не показывает ничего. Спрашивается после входа, а не при
         запуске: до входа человеку нечего сообщать, а отказ на пустом месте
         возвращается только через настройки телефона. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="KORKEM Flow"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Android 11 (API 30) hides other packages by default, so
             url_launcher cannot resolve an intent it has not declared here.
             Without these the Call, WhatsApp and Email actions on the deal,
             lead and customer screens silently do nothing. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>

        <!-- Служба распознавания речи. С Android 11 пакеты не видны друг другу
             по умолчанию, поэтому без этих двух объявлений приложение не найдёт
             распознаватель, даже когда он установлен и разрешение выдано.
             Два, а не одно: старые устройства отвечают на RecognitionService,
             новые — на ACTION_RECOGNIZE_SPEECH, и `speech_to_text` спрашивает
             оба. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH"/>
        </intent>
    </queries>
</manifest>
