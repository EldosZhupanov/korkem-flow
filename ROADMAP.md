# ROADMAP.md — KORKEM Flow

**Дата:** 2026-08-31 · **Горизонт планирования:** 12–18 месяцев для одного инженера.

Что строим и по каким законам — `PLAN.md`. Что прямо сейчас — `NOW.md`.

Порядок выбран по зависимостям, а не по амбициям. Каждый горизонт заканчивается
чем-то, что можно отдать в эксплуатацию: если ресурс кончится на Г4, у клиента
останется работающая система — узкая, но настоящая.

**Статусы:** ⬜ не начат · 🟨 в работе · ✅ сделан и проверен · `[~]` сделан, но не проверен

---

## Цепочка цифрового администратора — что строим и в каком порядке

**Добавлено 2026-09-03 и с этого дня главенствует над горизонтами ниже.**
Горизонты остаются как инженерный задел; порядок работ задаёт эта цепочка.

### Откуда она взялась

Из интервью с настоящим владельцем производства, а не из наших предположений.
Он описал потерю дословно:

> клиент звонит · владелец стоит за ЧПУ · записывает размер в блокнот ·
> продолжает работу · вечером должен перенести в таблицу · иногда забывает ·
> задача не уходит замерщику · заказ задерживается

И назвал, чего хочет: **«цифровой администратор, которому я могу сказать всё
голосом»**. Проверяемая цель продукта из этого следует одна: **владелец не
нанимает администратора.**

Внешняя экспертная оценка добавила второе: рынок конкурентный (Furnix, БАЗИС,
1С:УНФ, Bitrix24, Odoo), и одна функция продуктом не является. Цепочка должна
быть целой — иначе фабрике незачем менять то, что у неё есть.

### Правила, по которым эта цепочка строится

1. **Каждый этап оставляет продукт целиком рабочим.** Полусделанное звено хуже
   отсутствующего: оно создаёт ощущение системы, которая рвётся.
2. **Ни одно звено не требует ИИ для работы.** Модель ускоряет, а не включает.
3. **Всё, что нельзя написать кодом, названо явно** и вынесено в блокирующие
   решения владельца — не прячется внутри этапа.
4. **Числа для доказательства собираются с первого дня**, а не добавляются
   потом: поймано, передано человеку, стало работой, протухло.

### Этапы

| № | этап | состояние |
|---|---|---|
| **1** | Обращение → клиент → лид → замерщик с датой → напоминание | сервер захвата **сделан**, остальное в работе |
| **2** | Замер: время, адрес, фото и референсы | не начат |
| **3** | КП: предложение из замера, отправка ссылкой, подтверждение | не начат, `Quotation` в ERPNext есть |
| **4** | Договор и предоплата | **заблокирован** — см. ниже |
| **5** | Дизайн: задача со сроком, проверка результата, напоминание | не начат |
| **6** | Спецификация и BOM, в том числе импортом из БАЗИС | разведка сделана, кода нет |
| **7** | Склад, дефицит, заявка на закуп | **готово** |
| **8** | Производственные задания, очередь, уведомления, просрочка | **готово** |
| **9** | Брак и переделка | частично |
| **10** | Доставка и монтаж | доставка готова, монтажа нет |
| **11** | Остаточная оплата, акт, гарантия | **заблокирован** оплатой |

### Администрирование без админки ERPNext

Владелец не должен открывать панель ERPNext вообще. Она нужна монтажнику, а не
жильцу. Часть этого уже сделана и просто не была названа вслух.

**Уже в приложении, админка не нужна:**

| что | где |
|---|---|
| регистрация компании и владельца при первом запуске | `features/provisioning` — `ADR-0027` |
| ключ ИИ-провайдера, выбор модели, проверка соединения | `features/ai_settings` |
| токены Telegram и WhatsApp, состояние каналов | `features/channel_settings` |
| заказы, производство, склад, отгрузки, рабочие места | соответствующие экраны |

**Ещё требует админки — это и есть остаток этапа:**

| что | почему это важно владельцу |
|---|---|
| приглашение сотрудников и выбор должности | служба готова (`services/invitations.py`), экрана нет |
| реквизиты компании: адрес, БИН, банк | попадают в договор и в накладную |
| номенклатура и прайс-листы | без них нельзя выставить КП |
| склады и единицы измерения | заводятся один раз, но заводятся |
| роли конкретного человека после приглашения | сейчас только через панель |

Правило для этого этапа: **каждый пункт, ради которого владелец открыл админку,
считается дефектом продукта, а не особенностью настройки.**

### Что блокировано и чем именно

Это не наш код. Пока нет ответа — этапы 4 и 11 не начинаются.

| нужно | почему | что требуется от владельца |
|---|---|---|
| **Kaspi Pay** | приём предоплаты и остатка, QR и вебхук о поступлении | договор с Kaspi Bank; ИП или ТОО; счёт; ЭЦП. Без договора доступа к API нет вообще |
| **TrustMe / TrustContract** | подписание договора с юридической силой (SMS, QR через eGov, Face ID); у сервиса есть открытый API | учётная запись |

**Важное уточнение, снятое проверкой 2026-09-03:** TrustMe — это подписание, а
не платежи. Оплатить через него по Kaspi нельзя. Это два разных сервиса и две
разных интеграции, которые складываются, но не заменяют друг друга.

### Что уже решено и переспрашивать не нужно

Чтобы следующий агент не начинал с нуля:

* **ICP — не фабрика на 20–100 человек.** Это владелец, который сам стоит за
  станком: цех на 3–15 человек плюс ИП. Цифра 20–100 была выдумана и снята.
* **Компанию создаёт мастер ERPNext**, а не наша форма — `ADR-0027`.
* **Захват записывается до того, как понят**: сказанное — отдельная сущность,
  а не сразу лид. Решать, что это, в момент, когда решать некогда, значит
  воспроизвести блокнот.
* **Разбор модели хранится рядом с фразой, а не вместо неё.**
* **БАЗИС выгружает XML** со спецификацией, маршрутом и расценками; структура
  меняется между версиями, размеры приходят не круглыми (520 против 519.998),
  материалы сопоставлять только по коду — `docs/architecture/bazis_integration_study.md`.
* **Экран захвата в приложении отменён владельцем** 2026-09-03 и удалён.
  Серверная часть оставлена: через неё пойдут мессенджеры.

---

## Сводка

| | Горизонт | Оценка | Статус |
|---|---|---|---|
| **Г0** | Инженерный фундамент | 1 неделя | ✅ |
| **Г1** | KORKEM Service API | 3–5 недель | 🟨 |
| **Г2** | Узел и Desktop | 4–6 недель | 🟨 |
| **Г3** | Личность, организация, роли | 3–4 недели | ⬜ |
| **Г4** | Mobile, терминал цеха, очередь | 4–5 недель | ⬜ |
| **Г5** | Сотрудники и задачи | 3–4 недели | ⬜ |
| **Г6** | Каналы: Telegram и WhatsApp | 2–3 недели | ⬜ |
| **Г7** | Полный жизненный цикл заказа | 6–8 недель | ⬜ |
| **Г8** | Фасадный домен KORKEM | 8–10 недель | ⬜ |
| **Г9** | Финансы и зарплата | 5–6 недель | ⬜ |
| **Г10** | Проактивный AI | постоянно с Г1 | ⬜ |

**Г0–Г2 — фундамент всего продукта.** До них не строить функциональность.

---

## Г0 — Инженерный фундамент

**Зачем:** сейчас регресс обнаруживается человеком и с задержкой, а расход на
модель не ограничен ничем. Оба обязаны быть закрыты до реального трафика.

- [x] **Починить подготовку фикстур.** Три класса падают на
      `Duplicate entry 'Standard Buying'` в `setUpClass` на чистом volume:
      `test_agent_conversation`, `test_agent_conversation_message`,
      `test_pending_action`. Пока не починено, зелёный прогон недостижим.
- [x] **CI зелёный, проверен удалённо.** 2026-09-01, прогон
      `33537834277` — оба задания success за 18 минут:
      Flutter (analyze, format, 466 тестов) и Backend, который собирает стенд
      с нуля и гоняет `korkem_manufacturing` + `korkem_ai`.
      Дорога заняла четыре правки, и **ни одна не воспроизводилась локально**:
      чужой uid раннера в двух проявлениях (git «dubious ownership», затем
      `yarn` не может писать в вендорное дерево), мёртвая петля частично
      собранного bench, и кончившийся диск — о котором `yarn` сообщал
      по каждому пакету как о битых архивах.
      Первой же находкой CI стал дефект боевого пути: ненастроенный вебхук
      отвечал 500 вместо 401, а 500 провайдер повторяет.
- [x] **Учёт расхода модели.** `AI Usage Log` — строка на ход, с `request_id`
      как ключом идемпотентности. Учёт не может сломать работу: savepoint,
      и всё неопределённое читается внутри защиты (`usage.record_turn`).
- [x] **Потолок и лимит частоты.** `budget.check()` в `chat.send`,
      `chat.confirm` и канальном шлюзе. Site-wide `rate_limit` намеренно не
      используется — его ключ содержит только окно времени. Все лимиты по
      умолчанию нулевые, то есть безлимитные.
- [x] **Репетиция восстановления из бэкапа.** Проведена 2026-08-31 на чистом
      стенде: снимок восстановлен во временный сайт, мигрирован, сверён по
      счётчикам, структурным проверкам и конкретным записям, затем удалён.
      Основной сайт целью восстановления не был. Замеры, SHA-256 и границы —
      в `docs/operations/BACKUP_AND_RESTORE.md`. Нашла блокер: в образе не
      было `file`, без которого `bench restore` падает на свежем контейнере.
- [x] **Bootstrap на чистом volume после миграции в монорепо.** Прошёл. Нашёл
      склейку `apps.txt` и восемь самоотключающихся тестов изоляции компаний —
      см. `NOW.md`.

**Результат:** машина ловит регресс; счёт за модель ограничен и предсказуем;
бэкап разворачивали, а не верили в него.

**Горизонт закрыт полностью.** Машина ловит регресс на каждый пуш, счёт за
модель ограничен, бэкап разворачивали, а не верили в него.

---

## Г1 — KORKEM Service API

**Зачем:** сегодня 8 673 строки бизнес-логики живут в `korkem_ai/tools/`, и
`grep "frappe.whitelist" tools/*.py` даёт ноль. Значит любая запись стоит хода
модели, кнопки существовать не может, а Desktop и терминал построить нельзя.
**Это блокирует Г2, Г4, Г5 и Г6.**

Как именно переносить — скилл `korkem-domain-service`. Одно действие — один
коммит; `tools/production.py` это 2 096 строк, единый коммит не ревьюится.

- [x] **Слой домена в `korkem_manufacturing`.** Перенести логику из
      `production.py`, `procurement.py`, `buying.py`, `delivery.py`,
      `dispatch.py`, `capacity.py`, `orders.py`, `timeline.py`, `control.py`.
      Тела функций переносятся без изменений, вместе с тестами и комментариями.
- [x] **`@frappe.whitelist` API поверх домена.** Проверка прав, scope компании
      из сессии, запись в аудит. Типизировать: `require_type_annotated_api_methods`
      включён.
- [x] **Инструменты становятся обёртками.** `ToolSpec.handler` указывает на
      функцию API. Их собственные тесты обязаны пройти без правок — если тест
      пришлось менять, перенос изменил поведение.
- [~] **Приложение переходит на сервисы.** Производство и Склад **уже
      перешли**: `work_order_repository` и `stock_repository` зовут
      `korkem_manufacturing.api.queries`, и обе стороны фильтруют по компании
      через `services.scope.scoped()`.
      **Запись про дыру «`Work Order` идёт без фильтра по компании» устарела и
      снята 2026-09-02** — проверено в коде и тестом: реальный planner не
      находит задание чужой компании. Дыры нет.
      Осталось: у операций задания эндпоинт появился 2026-09-02, клиент на него
      ещё не переведён. Отдельно и позже — сущности CRM (клиенты, лиды, задачи,
      сделки, уведомления, согласования) по-прежнему ходят в
      `/api/resource/<Doctype>`; у них другой scope, и это не тот же вопрос.
- [x] **Кнопки на пять частых действий — все пять есть** (2026-09-02).
      Запустить производство · закрыть операцию · принять поставку · создать
      закупку · создать отгрузку. Каждая идёт через существующий репозиторий и
      существующий эндпоинт: второго пути мутации не заведено ни для одного.
      Все пять уходят в очередь при отсутствии сети и несут ключ
      идемпотентности, поэтому повтор не выполняет работу дважды.
      Экраны, которых для этого «не существовало», появились по дороге:
      заказы, задание с операциями, склад.
      Попутно выяснилось, что двум действиям не хватало не кнопки, а списка:
      без него приёмка просила вписать номер заказа поставщику руками.
      Добавлены `receivable_purchase_orders` и `orderable_material_requests`,
      и их фильтры повторяют условия отказа самих действий — список не
      предлагает того, что кнопка потом отвергнет.
- [ ] **Проверка R7:** каждое из пяти работает при выключенном LLM-провайдере.

**Результат:** продукт работает без модели. Открывается путь к Desktop,
терминалу и кнопкам в мессенджерах.

---

## Г2 — Узел и Desktop

**Зачем:** без узла, живущего отдельно от UI, нет локальной системы — есть
приложение к чужому серверу.

**Форма поставки решена** (2026-09-01): узел работает **в WSL2 на компьютере,
который у клиента уже стоит** — `ADR-0024`. Ничего не покупается; первый
клиент ставится в тот же день. Мини-ПК остаётся правильным вторым шагом.

**Роль облака решена** — `ADR-0025`: **облако передаёт, но не хранит.**
Владелец подтвердил, что мобильное приложение должно работать вне фабрики, и
это делает исходящий туннель обязательным, а не желательным.

Ограничение, которое никуда не делось: **Frappe не работает на Windows
нативно**. См. `PLAN.md` §3.2.

- [x] **Форма поставки узла** — `ADR-0024`, WSL2 на существующей машине.
- [x] **Роль облака** — `ADR-0025`, передаёт и не хранит.
- [~] **Установщик под Windows.** Скрипты есть:
      `infra/node/windows/Install-KorkemNodeAutostart.ps1` регистрирует задачу
      планировщика, поднимает дистрибутив и стек, ждёт `/health/ready`,
      переписывает проброс; `-Uninstall` снимает всё. Разбор, оба `-DryRun` и
      симметрия удаления проверены. **Регистрация задачи и срабатывание
      автозапуска — нет:** нужны права администратора и настоящая
      перезагрузка. Путь из шести шагов — `docs/operations/WINDOWS_NODE.md`.
- [~] **Проброс портов, переживающий перезагрузку.**
      `Update-KorkemNodeAccess.ps1` читает состояние из реестра (вывод
      `netsh show` переведён и разбору не подлежит) и отказывается настраивать
      проброс, если узел не отвечает по адресу WSL — иначе можно безупречно
      пробросить в пустоту. Владелец прогнал `-DryRun` на настоящей Windows:
      адрес определён, оба порта перечислены, ничего не изменено. **Создание
      правил не проверено** — нужны права администратора.
      Наложение `docker-compose.wsl-node.yml` проверено целиком: до него
      `<адрес WSL>:8000` не отвечал, после — 200.
- [ ] **Узел как служба.** Перезапуск при падении, живёт при закрытом UI.
      Health-точки уже есть: `/health`, `/health/ready`.
- [x] **Windows-таргет Flutter — собран.** `korkem_flow.exe` получен 2026-09-02
      и запущен: окно с формой входа, кириллица и Inter отрисованы. Рядом
      четыре плагинных DLL и `data/flutter_assets`.
      **Причина десяти «разных» ошибок оказалась одна и не та, что думали:**
      примерно 5% всех запусков процессов на этой машине падают с
      `0xC0000005`. Измерено (7 из 120 у `cmd.exe`), не выведено. Нехватка
      памяти, Defender, внедрённая DLL, Exploit Protection и осиротевший
      драйвер — все проверены и **отброшены**; остаётся аппаратная
      нестабильность. Собралось циклом повторов, потому что сборка
      инкрементальна между провалами.
      **Три прежних вывода были неверны и исправлены:** `chcp 65001` кодировку
      не лечит (за `FormatException` прятался крах потомка), `WHERE` падает не
      из-за недоступной записи PATH, а сам, и «код 5» — не права на каталоги.
      Разбор: `docs/operations/WINDOWS_DESKTOP.md`, раздел «Здоровье машины».
      Остаётся: пересобрать без `--no-tree-shake-icons` и прогнать ворота на
      здоровой машине.
- [ ] **Desktop UI как клиент своего узла** — по тому же API, что телефон.
- [x] **Бэкап наружу, в папку Windows.** `scripts/backup_offsite.sh` забирает
      архив из тома и кладёт на диск C, **сверяя каждый файл по SHA-256 с обеих
      сторон**: копия, которую никто не проверил, — это вера. Расхождение
      останавливает прогон и ничего не удаляет. Проверено 2026-09-02 настоящим
      прогоном: 1 883 274 байта базы легли на `C:\KorkemBackups`, Windows их
      видит, отказ без назначения даёт код 1, удержание из восьми наборов
      оставило три.
      **Это ещё не off-site:** диск C переживает порчу VHDX, но не кражу или
      пожар. Второе назначение — NAS или внешний диск — это второй вызов того
      же скрипта, а не новый код.
- [x] **Правило миграций.** Узел отказывается стартовать на данных новее
      своего кода. Версия — монотонное целое; маркер в двух местах (конфиг едет
      с config backup, строка в базе — с дампом), берётся максимум. Переменной
      обхода нет намеренно. Проверено настоящим отказом на подделанной версии.
- [ ] **Переписать политику приватности и ответы Data Safety** — до того, как
      релей поедет, а не после. Сейчас документ обещает «no other destination
      and no third party», и с релеем это становится неправдой: он не может
      прочитать содержимое, но получает его. «Не может прочитать» и «не
      получает» — разные утверждения, и ответы в Play Console юридически
      связывающие. Заметка вписана в сам документ.
- [~] **Исходящий туннель узел → облако.** **Спроектирован** — `ADR-0026`:
      узкий relay непрозрачных кадров, сквозной канал поверх него, привязка
      ключа локальным pairing, а не доверием облаку. Кода нет намеренно, и семь
      вопросов оставлены открытыми — начиная с выбора криптобиблиотеки.

**macOS.** У владельца есть MacBook M2, так что сборка реальна и требует
только Xcode; платный аккаунт Apple нужен лишь для передачи приложения
клиенту. Но узел на macOS — это Docker Desktop, а не WSL2. Отдельная работа,
после Windows.

**Результат:** предприятие ставит систему у себя, она переживает закрытый
ноутбук, и телефон видит её из города.

---

## Г3 — Личность, организация, роли

**Зачем:** до этого в системе один администратор. Дальше нужны семнадцать
разных людей с разными экранами.

- [ ] **Локальный вход обязателен и первичен:** email/пароль и PIN. Работают
      при выключенном интернете.
- [ ] **PIN-вход для цеха** — четыре цифры, разрешающиеся в настоящую сессию
      Frappe (ADR-0020 это уже решил, кода нет).
- [ ] **OAuth как удобство:** Google, Apple, Microsoft, телефон. Отображается
      на локального пользователя. Никогда не единственный путь. Discord — нет.
- [ ] **Организация и приглашения.** `OWNER` возникает при установке; остальные
      приглашаются с должностью и подразделением.
- [ ] **17 шаблонов ролей как версионируемые фикстуры** в
      `korkem_manufacturing` — не настройки, накликанные в Desk.
- [ ] **Стартовый экран по роли.** Фрезеровщик, бухгалтер и директор получают
      разные приложения из одного бинарника.
- [ ] **Серверная проверка R5:** пользователь не может повысить себе роль.
      Негативный тест обязателен.
- [ ] **Проверка прав уровня строки** за пределами Company и Customer —
      не подтверждена с Фазы 12.

**Результат:** цех, контора и директор пользуются системой одновременно и видят
каждый своё.

---

## Г4 — Mobile, терминал цеха, очередь

**Зачем:** телефон работника вне LAN — это не «офлайн узла», это отдельная
задача, и решать её надо дёшево.

- [ ] **Сопряжение устройства.** Код на Desktop → ввод на телефоне → телефон
      запоминает адрес узла и токен. Работник не вводит IP.
- [ ] **Поиск узла:** mDNS в LAN, фиксированный адрес в роутере, `10.0.2.2`
      для эмулятора, ретранслятор для внешнего доступа.
- [ ] **Исходящая очередь с идемпотентными ключами.** Действие ставится в
      очередь, воспроизводится при возврате связи, сервер отбрасывает дубликат.
      Примитив есть — `Pending Action` + `turn_id`. **Двунаправленную
      синхронизацию не строим.**
- [ ] **Выбрать локальное хранилище.** Сегодня его нет вообще: ни `sqlite`, ни
      `drift`, ни `hive`, ни `path_provider`.
- [ ] **Терминал цеха.** Планшет на LAN, крупные кнопки, список задач,
      прогресс до бонуса.
- [ ] **Честная деградация (R8).** Каждый внешний отказ говорит, что именно
      отвалилось — не спиннер.

**Результат:** работник у станка отчитывается с планшета; телефон в дороге не
теряет действие.

---

## Г5 — Сотрудники и задачи

**Зачем:** это то, чем директор пользуется каждый день.

- [ ] **Модуль «Команда».** Кто работает сейчас, сколько задач, по участкам.
- [ ] **Карточка работника.** Сегодня: операций, м², % плана. Месяц: м².
      Бонус. Брак. Среднее время операции.
- [ ] **Назначение работы** — один сервис `assign_work()`, из Desktop, из
      мобильного, из ассистента.
- [ ] **Дробление работы между 2–5 исполнителями** по процентам или по м².
- [ ] **Экран «Сегодня» и навигация от производства.** Сейчас нижнее меню —
      `Чат · Дашборд · Продажи · Задачи · Профиль`, сводка считает сделки и
      лиды, а производство спрятано в `/dashboard/production`.

**Результат:** директор раздаёт работу и видит выработку, не открывая Excel.

---

## Г6 — Каналы: Telegram и WhatsApp

**Зачем:** дёшево, потому что механика уже построена и протестирована в фазах
27–33: доставка с повторами, `Channel Event`, `turn_id`, кнопки подтверждения,
классификация отказов провайдера.

- [ ] **Один `Task`, три канала доставки.** Push, Telegram, WhatsApp — не три
      задачи. Кнопки `[Принять] [Не могу] [Открыть]` вызывают тот же сервис.
- [ ] **Публичный вход.** Домен, DNS, порты 80/443, выбор ingress: Caddy
      (написан и проверен через TLS) или Cloudflare Tunnel (нужен аккаунт).
      Полный список — `docs/operations/PRE_DOMAIN_CHECKLIST.md`.
- [ ] **Реальные учётные данные** Telegram и Meta. Оба провайдера уже
      достижимы из контейнера и корректно отвергают плейсхолдеры.
- [ ] **Привязка личности** Telegram/WhatsApp id к `User`.
- [ ] **Ограничение частоты на границе** — site-wide `rate_limit` Frappe для
      этого не годится, см. Г0.
- [ ] **Политика хранения `Channel Event`** — отсутствует с Фазы 31.

**Результат:** работник принимает задачу из Telegram, клиент спрашивает про
свой заказ и получает только свой.

---

## Г7 — Полный жизненный цикл заказа

**Зачем:** из 31 стадии `PROJECT.md` работают 14. Оба конца цепочки
отсутствуют, и денег в системе нет вообще.

- [ ] **Начало:** замер, фото помещения, дизайн, ревизия, утверждение
      клиентом. Фото — первый случай, когда системе нужны файлы от людей.
- [ ] **Договор и аванс.**
- [ ] **Упаковка** как операция маршрута — сейчас её нет между Сборкой и ОТК.
- [ ] **Монтаж, приёмка, гарантия, архив.** Установка и послепродажное
      обслуживание не имеют владеющей сущности — поднималось трижды на этапе
      архитектуры и не решено.
- [ ] **Отмена на каждой стадии.** Ни одной команды отмены не существует.

**Результат:** заказ живёт в системе от звонка до гарантии.

---

## Г8 — Фасадный домен KORKEM

**Зачем:** это то, за что платит заказчик, и этого нет. `Facade Item`, `Decor`
и `Milling Profile` заявлены в описании приложения и отсутствуют в коде.
Построена обобщённая корпусная фабрика: «Шкаф Астана», BOM в штуках.

Источник требований — `docs/product/korkem_flow_spec.md`.
Сущности уже описаны и обоснованы в `docs/architecture/domain_model.md`.

- [ ] **`Facade Item`** — высота × ширина × толщина в мм, количество, м² на
      позицию и на заказ. Площадь — единица, в которой этот цех считает всё.
- [ ] **Каталоги декоров** (KIRA, JS Group, Greenwood) с автодополнением плюс
      свои декоры, добавляемые на ходу.
- [ ] **Профили фрезеровки и кромки** на позиции.
- [ ] **Плёнка:** рулоны в п.м и м², списание при производстве, розничная
      продажа, деловой отход Д×Ш, предупреждение ниже порога.
- [ ] **Цветовые группы** — автоматически по декору и вручную по произвольному
      набору позиций. Видны на экране и на сменном задании.
- [ ] **Сменное задание** — только технические параметры, без цен и маржи.
- [ ] **Excel-лист утверждения** со спецификацией и блоком подписи.
- [ ] **Срочные заказы и VIP-клиенты.**

**Результат:** продукт можно показать KORKEM как их систему, а не как
демонстрацию абстрактной фабрики.

**Развилка:** если пилот идёт на самом KORKEM, этот горизонт поднимается перед
Г7. Смету и монтаж можно вести на бумаге ещё месяц; заказ в штуках вместо м²
цех не примет ни дня.

---

## Г9 — Финансы и зарплата

- [ ] **Деньги:** `Quotation` → `Sales Invoice` → `Payment Entry`; аванс и
      остаток на заказе.
- [ ] **Маржа на заказ** из `BOM.total_cost` против `Sales Order.grand_total` —
      обе цифры уже в базе, и никто их не сравнивает.
- [ ] **Экран бухгалтера:** поступления, расходы, счета, просрочка.
- [ ] **Сдельная оплата:** дневные и месячные пороги бонуса, авансы, штрафы за
      брак, период расчёта, выгрузка в PDF/Excel.
- [ ] **Права на финансы** — отдельная роль, негативный тест обязателен.

**Результат:** видно, зарабатывает ли фабрика, а не только сколько она делает.

---

## Г10 — Проактивный AI

Идёт параллельно, начиная с Г1: каждый новый сервис сразу получает инструмент.

- [ ] **Знания и память** (ADR-0019) — описано, кода нет.
- [ ] **Проактивный надзор.** Не «заказ просрочен», а «этот заказ сорвётся
      через четыре дня, потому что кромка кончится во вторник». Данные —
      загрузка станций, сроки, остатки — уже собраны.
- [ ] **Оценка качества ассистента.** Набор реальных вопросов с проверяемыми
      ответами, прогоняемый в CI при смене модели или промпта.
- [ ] **Решить судьбу двенадцати агентов** из `PROJECT.md`: в коде их два, а
      реальная архитектура — один цикл с вызовом инструментов, и он работает
      лучше. Либо строить, либо закрыть план.

**Результат:** управляющий узнаёт о срыве срока от системы, а не от клиента.

---

## Развилки, которые нельзя решить из кода

1. **Кто клиент первого релиза** — обобщённая корпусная фабрика или KORKEM с
   фасадами? От ответа зависит порядок Г7 и Г8.
2. **Форма поставки узла** — mini-PC, NAS или WSL2 на машине владельца?
   Блокирует весь Г2.
3. **Ingress** — Caddy (нужен публичный IP и порты) или Cloudflare Tunnel
   (нужен аккаунт)?
4. **iOS** — после пилота или в Г4? Нужен Mac, платный аккаунт и ревью;
   рекомендация — после.
5. **Кому принадлежит клиент — узлу или компании.** Обнаружено проверкой
   2026-09-03: у `Customer` в ERPNext нет поля компании, поэтому клиент, заведённый
   одной компанией, виден всем компаниям узла, включая его телефон в подсказках
   выбора. Сегодня это признано осознанным: клиент — общий справочник узла,
   и решение закреплено постоянным тестом, а не оставлено на память.

   Развилка возникает в день, когда на одном узле окажутся две компании одного
   владельца, не желающие видеть клиентов друг друга. Вывести это из
   существующей схемы нельзя: понадобится либо поле компании на клиенте, либо
   разделение по User Permission, и оба варианта задевают все продажи.

   Пока фабрика одна — не мешает ничему.

6. **Что значит «компания» для сущностей CRM.** Обнаружено и доказано
   2026-09-02: пользователь одной компании читает `CRM Organization`, `CRM
   Lead`, `CRM Deal` и `CRM Task` другой. Причина не в ошибке кода — **у этих
   доктайпов нет поля компании вообще**, поэтому фильтровать не по чему.
   Дыра записана исполняемым тестом
   `korkem_ai/test_resource_company_isolation.py` (четыре `expectedFailure`).

   Развилка настоящая, и вариантов три:

   * **Добавить компанию как поле** — честно и дорого: миграция, правка
     вендорных доктайпов через custom fields, и вопрос, что делать с уже
     существующими записями.
   * **Выводить компанию из владельца или ответственного** — дёшево, но лид
     без назначенного ответственного оказывается ничьим, а перевод человека
     между компаниями переписывает историю.
   * **Признать CRM общим для всех компаний владельца** — законно, если
     фабрика одна; становится дырой в тот день, когда их две.

   Пока решение не принято, **нельзя открывать раздел CRM пользователям
   разных компаний на одном узле.** Для одной фабрики это не мешает ничему.
