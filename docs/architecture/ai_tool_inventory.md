# Реестр AI-инструментов: статическая опись

Дата: 2026-09-13. Получено из ToolSpec AST рабочего дерева. Это декларации кода,
не доказательство исполнения всех handlers. API adapters показаны как в коде;
legacy handler без API не считается автоматически доменным сервисом.

Исходных деклараций: 65. Добавлены 4 инструмента ниже (регистрируются общим
helper, поэтому не входят в буквальный AST-поиск `ToolSpec(name="…")`).

| Tool | Purpose | Read/Write | Risk | Confirmation | Underlying domain service |
|---|---|---|---|---|---|
| manufacturing.calculate_facade_area | Площадь фасадов, mm → m² | Read | READ | Нет | manufacturing.api.calculations → services.calculations.calculate_facade_area |
| manufacturing.calculate_panel_area | Площадь панелей, mm → m² | Read | READ | Нет | manufacturing.api.calculations → services.calculations.calculate_panel_area |
| manufacturing.calculate_edge_length | Длина выбранных кромок, mm → m | Read | READ | Нет | manufacturing.api.calculations → services.calculations.calculate_edge_length |
| manufacturing.calculate_material_quantity | Нижняя оценка числа листов по площади | Read | READ | Нет | manufacturing.api.calculations → services.calculations.calculate_material_quantity |

| Tool | Purpose | Read/Write | Risk | Confirmation | Handler / underlying path |
|---|---|---|---|---|---|
| chain.accept_proposal | Turn a quotation the customer agreed to into a sales order. The delivery date is required: an order without a date is a promise nobody made. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [accept_proposal](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:366) → `services.acceptance.accept` |
| chain.assign_design | Assign the drawing for an order, with a deadline. A task without a date is a wish. The designer is a user id — find it with crm.search_users. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [assign_design](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:432) → `services.design.assign` |
| chain.capture_stats | How much of what was said got caught, handed over, turned into an order, dismissed — and how much went stale. The numbers that show whether the owner still needs an administrator. | Read | Risk.READ | No | [capture_stats](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:672) |
| chain.catalogue_items | Catalogue items with their prices. A null price means the price has not been worked out yet, which is normal for made-to-order furniture. | Read | Risk.READ | No | [catalogue_items](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:586) → `services.catalogue.items` |
| chain.catalogue_units | Units of measure worth offering for furniture — seven, not the two hundred and forty ERPNext knows. Call this before creating an item. | Read | Risk.READ | No | [catalogue_units](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:571) → `services.catalogue.units` |
| chain.complete_installation | Close the installation. Notes are optional but stay on the order — a year later, during a warranty claim, they are worth more than the fact that it was closed. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [complete_installation](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:496) → `services.installation.complete` |
| chain.contract_status | Whether the contract for a sales order exists, is signed, by whom and when. | Read | Risk.READ | No | [contract_status](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:183) → `services.contract.status` |
| chain.convert_capture | Turn a written-down note into an enquiry with a customer. Names an existing customer if one matches, or creates one. Refuses when more than one customer matches — an ambiguous customer is a question for the owner, not a guess. The user must confirm before this runs. | Write | Risk.WRITE | Yes (R10) | [convert_capture](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:125) → `services.enquiry.convert` |
| chain.create_item | Add a catalogue item. The unit must come from chain.catalogue_units — it is printed on the delivery note and the customer reads it. Price is optional. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [create_item](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:607) → `services.catalogue.create` |
| chain.deliver_design | Accept the design as done. Refused unless a file is attached to the order: a drawing that was spoken about is not a drawing. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [deliver_design](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:456) → `services.design.deliver` |
| chain.draft_contract | Draft the contract for a sales order. Without terms, it lists what was agreed — the legal wording is the company's own. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [draft_contract](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:389) → `services.contract.draft` |
| chain.draft_invoice | Draft the invoice for an order. Refused unless something has shipped: invoicing furniture that has not arrived is the fastest way to lose a happy customer. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [draft_invoice](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:555) → `services.invoicing.draft` |
| chain.draft_proposal | Draft a quotation for an enquiry. Items are required — an empty quotation is refused. Each item is {item_code, qty, rate}; find item_code with chain.catalogue_items. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [draft_proposal](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:338) → `services.proposal.draft` |
| chain.enquiry_candidates | Customers a spoken name could mean, for a person to choose between. Call this when converting a note refused because the name was ambiguous. | Read | Risk.READ | No | [enquiry_candidates](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:652) → `services.enquiry.candidates` |
| chain.record_capture | Write down what the owner just said, before it is lost — a call, a walk-in, a message. This is the note that used to live in a paper notebook. Give the words as spoken; add customer_hint if a name was said, assign_to (a user id) if somebody should measure, and due_on as YYYY-MM-DD if a day was named. The user must confirm before this runs. | Write | Risk.WRITE | Yes (R10) | [record_capture](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:91) → `services.capture.record` |
| chain.record_measurement | Record what the measurer found on site: dimensions as spoken, notes, and the address. The address becomes a real address on the customer, because delivery and installation will look for it there. Closes the measurer's task. The user must confirm before this runs. | Write | Risk.WRITE | Yes (R10) | [record_measurement](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:153) → `services.measurement.record` |
| chain.schedule_installation | Schedule the installation crew. Refused until something has shipped — a crew arriving without the furniture loses a day and the customer's trust. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [schedule_installation](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:472) → `services.installation.schedule` |
| chain.set_item_price | Name the price of a catalogue item, in the price list a quotation reads from. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [set_item_price](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:633) → `services.catalogue.set_price` |
| chain.sign_contract | Record that a contract was signed, by whom and when. A signature without a name is refused. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [sign_contract](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:409) → `services.contract.sign` |
| chain.warranty_claim | Accept a warranty claim — the customer calls, a hinge came off. Refused with the expiry date when the warranty has run out. The complaint text is required. The user must confirm. | Write | Risk.WRITE | Yes (R10) | [warranty_claim](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:531) → `services.warranty.claim` |
| chain.warranty_status | Warranty on an order: from which day, until which, and whether it still holds. Counted from the first shipment, not from the order. | Read | Risk.READ | No | [warranty_status](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:516) → `services.warranty.status` |
| chain.what_needs_attention | What is stuck today, in four lists: things said and handed to nobody, overdue measurements and installations, submitted orders with no design assigned, and furniture delivered but not invoiced. This is the question a factory owner asks every morning. | Read | Risk.READ | No | [what_needs_attention](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/chain.py:202) → `services.attention.today` |
| crm.create_lead | Create a new lead in the CRM. Requires the person's first name. Ask the user for anything you do not know rather than inventing it — an invented phone number is worse than a missing one. The user must confirm before this runs. | Write | Risk.WRITE | Yes (R10) | [create_lead](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:453) |
| crm.create_task | Create a task in the CRM, optionally assigned to a colleague, with a due date, and attached to a deal, lead or organization. Requires a title. Look up the assignee with crm.search_users first — assignment is by user id, not by name. The user must confirm before this runs. | Write | Risk.WRITE | Yes (R10) | [create_task](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:674) |
| crm.customer_timeline | Everything about one customer in one call: their CRM deal, orders, production progress, material shortages, purchasing, receiving and deliveries, with the real links between them. Use for «покажи всё по клиенту X», «что происходит с заказом X», «есть ли проблемы у X», «сколько уже изготовлено». A section that says none has no such document; a section that says no_access means the user cannot see it — do not report the second as the first. | Read | Risk.READ | No | [customer_timeline](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/timeline.py:405) |
| crm.get_deal | Read one deal by its id, e.g. CRM-DEAL-2026-00001. | Read | Risk.READ | No | [get_deal](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:267) |
| crm.search_deals | Search deals in the CRM. Returns the caller's own deals only — this CRM restricts deals to their owner and assignees. | Read | Risk.READ | No | [search_deals](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:246) |
| crm.search_leads | Search leads in the CRM. | Read | Risk.READ | No | [search_leads](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:296) |
| crm.search_organizations | Search customer organizations by name. | Read | Risk.READ | No | [search_organizations](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:282) |
| crm.search_users | Find colleagues by name, to get the user id needed to assign a task. Use this before assigning work to somebody named in the request. | Read | Risk.READ | No | [search_users](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:530) |
| dispatch.assign_work | Send an employee an instruction about an order or a work order, through whichever chat channel they are linked on, and record it. Use for «передай Ивану…», «сообщи Ивану, что…». Does not start or change production — the manufacturing tools do that. | Write | Risk.WRITE | Yes (R10) | [assign_work](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/dispatch.py:467) |
| dispatch.list_instructions | Instructions given to employees and what they answered — who has accepted, who has not replied, who refused. Answers «какие задания не приняты». | Read | Risk.READ | No | [list_instructions](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/dispatch.py:495) |
| dispatch.respond_to_instruction | Record that you accept or refuse a job you were given — «принял», «не могу выполнить». Names the instruction only when more than one is open. | Write | Risk.WRITE | Yes (R10) | [respond_to_instruction](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/dispatch.py:519) |
| inventory.create_material_request | Raise one purchase request for material that is short. Pass sales_order to buy for one order, or omit it to buy everything the factory is missing in a single request. Ask inventory.material_shortage or inventory.factory_shortage first and pass the quantities it reported. Requires the user to confirm before anything is created. | Write | Risk.WRITE | Yes (R10) | [create_material_request](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/procurement.py:873) |
| inventory.factory_shortage | What the whole factory is short of, across every active order. Use this for «что не хватает на складе», «какие материалы нужно купить», «какой материал самый критичный» or «какие заказы блокируются из-за материалов». For one specific order use inventory.material_shortage instead. Every quantity, severity and date here is computed from the database — report them as given and do not add, compare or re-derive any of them. | Read | Risk.READ | No | [factory_shortage](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/procurement.py:802) |
| inventory.get_stock | Stock for items: on hand, reserved for production, on order, and projected. Use projected_qty to judge what is actually free. | Read | Risk.READ | No | [get_stock](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/erp.py:388) |
| inventory.material_shortage | What one named sales order is short of. Returns every material that order needs with required, available, reserved and short quantities. Use inventory.factory_shortage instead when the question is about the warehouse or the factory rather than a specific order. Use these numbers rather than your own arithmetic. | Read | Risk.READ | No | [material_shortage](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/procurement.py:843) |
| inventory.receive_purchase_order | Book a delivery in against a purchase order, which puts the material on the shelf and unblocks production. Use when the user says the goods have arrived — «прими поставку», «материал пришёл». Quantities come from the order; pass items only for a partial delivery, and never a number the user did not give you. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [_api().receive_purchase_order](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/buying.py:675) |
| manufacturing.capacity | How loaded each workstation is: hours of work queued against a day of its own capacity, and which station is the bottleneck. Use for «какая загрузка производства», «какие станки перегружены», «как загружен ЧПУ». Report the numbers as given; utilisation above 100% means more than a day's work is waiting, not that the station is overbooked today. | Read | Risk.READ | No | [factory_capacity](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/capacity.py:355) |
| manufacturing.complete_operation | Record that a production stage is finished — «раскрой закончен», «кромление готово», «сделали 4, одну в брак». Pass qty only if the user said how many were good; otherwise everything still outstanding is taken. Pass scrap_qty when the user says pieces were spoiled, rejected or written off — never fold them into qty. The stage stays open until every piece is accounted for, so a partial run can be finished later. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [_api().complete_operation](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:1024) |
| manufacturing.complete_production | Release finished goods from a work order — «производство закончено», «выпусти готовую продукцию», «мы изготовили 5 штук». This is what consumes the raw material and puts finished units on the shelf; completing an operation does not. Pass qty only if the user said how many. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [complete_production](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:1189) |
| manufacturing.complete_rework | Close a rework that was sent with complete_operation — «исправление завершено», «починили», «не удалось исправить, в брак». Records the rework effort and returns the piece to good output, or writes it off if it could not be saved. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [complete_rework](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:1110) |
| manufacturing.get_bom_materials | The raw materials needed to build a quantity of an item, from its active BOM, fully exploded through sub-assemblies. | Read | Risk.READ | No | [get_bom_materials](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/erp.py:343) |
| manufacturing.production_control | What is happening in production right now: every active sales order with its work orders, progress, delivery date, whether it is overdue, and whether material is short. Use this for questions like «что сейчас происходит в производстве», «какие заказы просрочены» or «какой заказ заблокирован». Report its numbers as given — do not recount or re-derive them. | Read | Risk.READ | No | [production_control](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/control.py:240) |
| manufacturing.production_priority | What to start first, and why. Returns an ordered queue with a one-sentence reason for each position, plus the orders that cannot start at all because material is missing. Use for «что запускать первым», «почему этот заказ первый», «какие заказы под угрозой». Give the reasons as written — they are the answer, not a score. | Read | Risk.READ | No | [production_priority](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/capacity.py:375) |
| manufacturing.record_inspection | Record the quality-control verdict on a production stage — «ОТК принял», «ОТК забраковал», «контроль пройден». Only stages the bill of materials marks for inspection have one; for the rest this reports that none is needed. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [record_inspection](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:1154) |
| manufacturing.search_work_orders | Work orders on the shop floor, optionally for a sales order, an item or a status (Not Started, In Process, Completed, Stopped). | Read | Risk.READ | No | [search_work_orders](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/erp.py:365) |
| manufacturing.shop_floor | What every workstation is doing right now: which operation is running, what is queued behind it, and how much of each is done. Use for «что сейчас на станке», «сколько сделано», «что сейчас выполняется». Report its numbers as given. | Read | Risk.READ | No | [shop_floor](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:973) |
| manufacturing.start_operation | Record that a production stage has begun — «начали кромление», «запустили раскрой». Name the operation and the order; the job card is found from them. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [start_operation](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:997) |
| manufacturing.start_production | Put a sales order into production: plan the work order if there is none, then move the material into work-in-progress. Use when the user says «запусти производство», «начни делать», and also «подай материал» or «продолжи производство» for a job that is already running but has consumed what was transferred to it — the next batch needs its own material before it can be built. Refuses if the material is not physically on the shelf — ordered is not received. Requires confirmation before anything moves. | Write | Risk.WRITE | Yes (R10) | [_api().start_production](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:940) |
| manufacturing.stop_production | Halt a job or put it back to work — «останови производство», «приостанови заказ», «возобнови производство», «продолжай этот заказ». Stopping frees the material the job had reserved so other orders can use it; nothing already made or consumed is undone. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [stop_production](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/production.py:1076) |
| procurement.create_purchase_order | Place a purchase order with a supplier for what a material request still needs. Supplier and price come from ERPNext — never invent either; if the tool reports one is missing, tell the user and stop. Requires the user to confirm before anything is sent. | Write | Risk.WRITE | Yes (R10) | [_api().create_purchase_order](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/buying.py:712) |
| procurement.procurement_status | The whole purchasing picture at once: open requests, what is on order, what is overdue, what arrives soon, and which shortages are holding production up with the stage each one has reached. Use for «что происходит с закупками», «что блокирует производство», «что требует внимания в закупках». Prefer this over calling several procurement tools in a row. | Read | Risk.READ | No | [procurement_status](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/buying.py:612) |
| procurement.search_material_requests | Purchase requests and how much of each is still not ordered. Use for «какие заявки на закупку открыты», «что ещё не заказано», «по каким материалам нет заказа поставщику». For what is already on order use procurement.search_purchase_orders. | Read | Risk.READ | No | [search_material_requests](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/buying.py:553) |
| procurement.search_purchase_orders | What is on order from suppliers, what has arrived, and when the rest is due. Use for «что заказано», «что придёт на этой неделе», «покажи просроченные закупки». Ordered is not the same as received — report both as given. | Read | Risk.READ | No | [search_purchase_orders](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/buying.py:581) |
| procurement.search_receipts | Deliveries that have actually arrived, with what was received into which warehouse. Use for «что пришло сегодня», «пришла ли ДСП 16мм», «пришёл ли заказ от поставщика X». For what is still expected use procurement.search_purchase_orders instead — arrived and expected are opposite questions. | Read | Risk.READ | No | [search_receipts](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/buying.py:643) |
| profile.current_user | Who the assistant is acting as, and which roles they hold. | Read | Risk.READ | No | [current_user](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:352) |
| sales.create_delivery | Ship finished goods to the customer against a sales order — «отгрузи», «отправь заказ». Quantities come from what is still owed and what is on the shelf; pass items only if the user named specific ones, and never a number they did not give you. Partial shipments are normal. Requires confirmation. | Write | Risk.WRITE | Yes (R10) | [_api().create_delivery](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/delivery.py:216) |
| sales.create_sales_order | Place an order for a customer: one item, a quantity and a requested delivery date (YYYY-MM-DD). The customer is decided by the server for a customer's own session — never pass one on their behalf. Returns status 'incomplete' naming what is missing, or 'ambiguous' with candidates, rather than guessing. | Write | Risk.WRITE | Yes (R10) | [create_sales_order](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/orders.py:595) |
| sales.delivery_forecast | When an order can realistically be ready, from the work that remains, against the date on the order. Answers «когда будет готов». | Read | Risk.READ | No | [delivery_forecast](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/orders.py:623) |
| sales.delivery_status | What can be shipped to customers: how much of each order is still owed, how much is on the shelf, what is blocking the rest, and the delivery notes already sent. Use for «что готово к отгрузке», «что нужно доставить сегодня», «сколько осталось доставить», «можно ли отгрузить заказ X», «что уже доставили». Report its numbers as given. | Read | Risk.READ | No | [delivery_status](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/delivery.py:190) |
| sales.get_sales_order | One sales order with the items on it, by id, e.g. SAL-ORD-2026-00001. | Read | Risk.READ | No | [get_sales_order](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/erp.py:327) |
| sales.search_items | What the factory makes and can be ordered — the item catalogue, optionally narrowed by name. Use before creating an order when the customer's wording does not match an item exactly. | Read | Risk.READ | No | [search_items](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/orders.py:571) |
| sales.search_sales_orders | Find submitted sales orders, optionally by customer name or status (To Deliver and Bill, To Deliver, To Bill, Completed, Closed). | Read | Risk.READ | No | [search_sales_orders](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/erp.py:305) |
| tasks.list | List CRM tasks. Use overdue=true for tasks past their due date that are not finished, and assigned_to_me=true for the caller's own. | Read | Risk.READ | No | [list_tasks](../../backend/korkem_ai/korkem_ai/korkem_ai/tools/catalog.py:314) |
